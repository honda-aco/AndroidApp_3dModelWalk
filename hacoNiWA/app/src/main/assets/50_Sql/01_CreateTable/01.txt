CREATE TABLE IF NOT EXISTS CHARA_START_COUNT (CHARAID text, STARTCOUNT integer, primary key(CHARAID));CREATE TABLE IF NOT EXISTS CHARA_TALK_MAP (CHARAID text, TALKID text, primary key(CHARAID));CREATE TABLE IF NOT EXISTS CHARA_TALK (TALKID text, SEQ integer, TALK_TEXT_JPN text, TALK_TEXT_ENG text, primary key(SEQ));CREATE TABLE IF NOT EXISTS SETTING (ID text, VALUE integer, primary key(ID));CREATE TABLE IF NOT EXISTS SETTING_REAL (ID text, VALUE real, primary key(ID));CREATE TABLE IF NOT EXISTS CHARA_WALK_BITMAP (CHARAID text, SIZE integer, DIRECTION integer, NO integer, SUBNO integer, BITMAP blob, primary key(CHARAID, SIZE, DIRECTION, NO, SUBNO));CREATE TABLE IF NOT EXISTS CHARA_POSE_BITMAP (CHARAID text, POSEID integer, SIZE integer, NO integer, SUBNO integer, BITMAP blob, primary key(CHARAID, POSEID, SIZE, NO, SUBNO));